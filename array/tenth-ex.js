// Hai un array di numeri numeri = [1, 2, 3, 4, 5]. 
// Utilizza il metodo map() per creare un nuovo array in cui ogni numero sia moltiplicato per 2. Stampa il nuovo array.


const numeri = [1, 2, 3, 4, 5];

const numeriMolt = numeri.map(numero => numero * 2);
console.log(numeriMolt)