// Dato l'array prodotti = ["computer", "mouse", "tastiera", "monitor"], scrivi del codice che verifichi se la stringa "tastiera" è presente nell'array.
// Stampa un messaggio appropriato che indichi se l'elemento è stato trovato o meno.

// const prodotti = ["computer", "mouse", "tastiera", "monitor"];

// const prodottoCercato = "tastiera";

// if (prodotti.includes(prodottoCercato)) {
//     console.log(`l'elemento ${prodottoCercato} è presente nell'array`);

// } else {
//     console.log(`l'elemento ${prodottoCercato} non è presente nell'array`);
// }