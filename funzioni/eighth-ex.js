// Crea una funzione chiamata trovaMassimo che prende due numeri come argomenti e restituisce il numero più grande.

function trovaMassimo(num1, num2) {
    if (num1 > num2) {
        return num1

    } else {
        return num2
    }
}


console.log(trovaMassimo(5, 8));