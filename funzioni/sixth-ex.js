// Crea una funzione chiamata calcolaMediaPonderata che accetta tre parametri: voto1, peso1, voto2, peso2.
// La funzione deve calcolare la media ponderata dei due voti utilizzando la seguente formula:

// Media= (voto1×peso1)+(voto2×peso2) / peso1+peso2

// La funzione deve restituire il valore della media ponderata.

// function calcolaMediaPonderata(voto1, peso1, voto2, peso2) {
//     return ((voto1 * peso1) + (voto2 * peso2)) / (peso1 + peso2)
// }

// console.log(calcolaMediaPonderata(7, 0.4, 9, 0.6));