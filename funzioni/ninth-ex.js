// Raddoppia i numeri

// Crea una funzione freccia che prende un numero come parametro e restituisce il doppio del numero.

// Usa la funzione per raddoppiare il numero 7 e stampare il risultato.

const numDoppio = (num) => num * 2;
console.log(numDoppio(7));